import { render } from "@testing-library/react";
import App from "./App";

// PUBLIC_INTERFACE
test("renders Main App component", () => {
  render(<App />);
  // Renders game setup/player selection
});
